upstream backend {
    server backend:8000;
}

server {
    listen 8080;
    server_name localhost;

    location = /favicon.ico { access_log off; log_not_found off; }
    
    location /static/ {
        proxy_pass http://backend;
    }

    location / {
        proxy_pass http://backend;
    }
}
